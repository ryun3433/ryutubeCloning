extends layouts/main

block content
    .form-container
        form(action=`/videos${routes.upload}`, method="post")
            label(for="file") VideoFile
            input(type="file" id="file", name="file ", recuired=true)
            input(type="text" name="title" placeholder="Title", recuired=true)
            textarea(name="description" placeholder="Description", recuired=true)
            input(type="submit" value="Upload Video")